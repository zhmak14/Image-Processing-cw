The code is run by: 
python main.py ./input_directory_name/

Upon running Results directory is created if not existent and output images
are saved there.

The file corner_find.py does not need to be run in order for the solution
to work. It exists only to show and to prove the way warped images' corners 
were found manually to be hardcoded into the dewarping function.
